Deployment audit done for tomorrow (April 5, 2026) setup.
RTSP is supported in code, but current project state is not farm-live ready yet without a few fixes/config updates.
    1. RTSP Compatibility Verdict
    2. edge_detector + video_stream support RTSP, NVR_CHANNEL, and FILE streams (edge_detector.py, video_stream.py).
    3. Current cached runtime config is in FILE mode (local mp4 paths), not live RTSP (local_cache.json).
    4. Runtime config is DB-driven via /api/v1/edge/runtime-config (edge_runtime_config_api.py).
    5. Critical Risks To Fix Before Farm Deployment
    6. systemd/workforce-edge.service does not run config sync and has wrong precheck path (/home/neopeak/edge_precheck.sh) (workforce-edge.service, edge_precheck.sh).
    7. Live-stream reconnect block is effectively bypassed by an early continue on frame read failure (in live mode) in camera loop (edge_detector.py).
    8. Backend runtime-config does not provide decode_mode; code defaults AUTO -> CPU decode, so multi-camera live load may choke (edge_runtime_config_api.py, video_stream.py).
    9. Env/doc naming mismatch: runtime uses EDGE_DEVICE_KEY, old docs still say EDGE_TOKEN (edge_detector.py, jetson_info.txt).
    10. BACKEND_API_URL confusion: edge_config_sync appends /api/v1/...; if you set URL already ending /api/v1, it can duplicate path (edge_config_sync.py).
    11. backend/metadata.json is incompatible with current runtime-config contract (class_map required) if reused directly (metadata.json, edge_runtime_config_api.py).
    12. New Farm Onboarding Checklist (Code + DB + Jetson)
    13. Add farm row in farm with correct timezone (used by resolver/aggregation).
    14. Add cameras in farm_camera with stream_type, rtsp_url or nvr_rtsp_base+nvr_channel.
    15. Add camera_stream_config rows (fps_target, resolution, optional roi/motion).
    16. Add camera_activity_zone for each camera/activity (FEEDING, SCRAPPING) with ROI JSON.
    17. Ensure activity_type codes in DB match edge payload names (SCRAPPING spelling matters).
    18. Add activity schedules in activity_schedule for that farm/activity.
    19. Provision device: run device_provisioning.py with new farm_id, save API key.
    20. Assign model to device via model_registry_sync.py (or existing assignment flow).
    21. Jetson env update (backend/.env): EDGE_API_BASE, EDGE_DEVICE_KEY, DEVICE_CODE, DATABASE_URL (backend host side), queue/fps knobs.
    22. Jetson bootstrap update: bootstrap_config.json backend_api_url must be new backend IP/domain.
    23. Run config sync once and verify new local_cache.json now contains RTSP/NVR camera config.
    24. Start services in order and verify: backend -> edge detector -> heartbeat -> watchdog -> aggregator -> phase5 timer.
    25. One-Time IP/WiFi Change Items
    26. Update backend host IP/domain in Jetson bootstrap_config.json (backend_api_url).
    27. Update EDGE_API_BASE in backend/.env on Jetson side to http://<new-backend-ip>:8000/api/v1.
    28. If camera/NVR network changed, update farm_camera.rtsp_url or nvr_rtsp_base in DB.
    29. Regenerate/replace device key only if provisioning new Jetson device.
    30. Important Clarification
    31. I could not run a real live RTSP stream test from this environment (no camera/network access), so this is a static code/config audit.
    32. For new farm onboarding, mainly DB + env changes are needed; code changes are only needed to address the critical risks above.
If you want, I can now make the exact code fixes for the 3 main blockers (service precheck path/startup flow, live reconnect bug, decode_mode/runtime-config improvement) so you can deploy with a safer build tomorrow.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Your checklist is strong, and yes, we should act on parts of it.
Based on current code, here is the exact verdict:
    1. FRAME_QUEUE backpressure
    2. Partially already implemented.
    3. Current code uses bounded queue (FRAME_QUEUE_MAXSIZE, default 20) and drops new frames when full.
    4. Missing piece: it does not actively drop oldest per camera in the normal enqueue path.
    5. Recommendation: implement improved drop policy soon (high priority), but not a blocker for tomorrow if load is moderate.
Reference: edge_detector.py
    6. Camera starvation / fairness
    7. Your concern is valid with single FIFO.
    8. Current architecture can starve slower/later cameras under pressure.
    9. Recommendation: implement per-camera queues + round-robin worker after farm dry run (medium-high priority).
Reference: edge_detector.py
    10. Latency vs FPS control
    11. Already partially present via time gating and frame skip logic.
    12. Not yet true end-to-end latency-aware scheduling.
    13. Recommendation: keep current for tomorrow, then add explicit latency budget policy in next patch.
Reference: edge_detector.py
    14. CPU decode limit
    15. Fully valid.
    16. We already added explicit decode_mode in your local_cache.json (1 GPU + 4 CPU).
    17. Given current MAX_GPU_STREAMS=1, this is the safest immediate setup.
    18. Recommendation: monitor and possibly raise GPU-decoded streams to 2 after real farm profiling.
References: local_cache.json, video_stream.py
    19. Stream recovery
    20. You are right this is critical.
    21. Current code has reconnect logic, but live-read failure path has a flow issue that can bypass reconnect attempts.
    22. Recommendation: fix before/at deployment window (high priority).
Reference: edge_detector.py
    23. Timestamp drift
    24. On this point, your proposed datetime.utcnow() alone is not enough.
    25. Best practice is exactly what you said next: capture timestamp at frame acquisition and propagate it through pipeline.
    26. Current code mixes wall-clock usage in multiple places.
    27. Recommendation: implement unified frame timestamp propagation (medium priority, but very important for analytics correctness).
References: edge_detector.py, activity_aggregator.py
Should we implement now?
    1. Implement now (before farm go-live): stream recovery fix.
    2. Implement next (soon after first farm run): queue fairness + improved drop policy.
    3. Implement after stability baseline: full timestamp propagation refactor.

------------------------------------------------------------------------------------------------------------------------------------------

Use this tomorrow-safe config for first farm rollout (5 cameras, April 5, 2026).
    1. Jetson .env (recommended values)
# Connectivity EDGE_API_BASE=http://<BACKEND_IP>:8000/api/v1 EDGE_DEVICE_KEY=<DEVICE_API_KEY> DEVICE_CODE=<DEVICE_CODE> BACKEND_API_URL=http://<BACKEND_IP>:8000 # Runtime stability EDGE_PROCESSING_FPS=4 EDGE_FRAME_QUEUE_MAXSIZE=12 EDGE_EVENT_QUEUE_MAXSIZE=4000 EDGE_SENDER_THREADS=3 EDGE_LOG_LEVEL=INFO # Watchdog EDGE_DETECTOR_SERVICE_NAME=workforce-edge.service EDGE_WATCHDOG_STARTUP_GRACE_SEC=120 EDGE_WATCHDOG_NO_PROGRESS_CHECKS=4 # Heartbeat HEARTBEAT_INTERVAL=120 # Aggregator safety AGG_BATCH_SIZE=300 AGG_MAX_SESSION_AGE_SEC=3600 AGG_PENDING_LOG_EVERY_LOOPS=10 
    2. Camera decode split (already set in your cache)
    3. Camera 1: GPU
    4. Cameras 2–5: CPU
    5. Before starting services (must pass)
    6. Backend reachable: curl http://<BACKEND_IP>:8000/health
    7. Runtime config fetch works with device code.
    8. All 5 cameras have RTSP and non-null fps (set missing one to 10).
    9. local_cache.json is fresh (not stale test config).
    10. Start order
    11. Backend
    12. Aggregator service
    13. Phase5 timer
    14. Edge detector
    15. Heartbeat
    16. Watchdog
    17. First 30-minute live checks
    18. FrameQ should not stay near max for long.
    19. EventQ should not grow continuously.
    20. Each camera shows non-zero FPS in logs.
    21. No repeated “Frame read failed” loops.
    22. Heartbeats arrive every ~120s.
    23. Immediate rollback knobs if overloaded
    24. Reduce EDGE_PROCESSING_FPS from 4 to 3.
    25. Keep only 1 GPU decode stream.
    26. Temporarily disable weakest camera feed until stable.
If you want, I can next give you a one-page “farm-day command sheet” (exact commands to run in order)

----------------------------------------------------------------------------------------------------------------

Farm-Day Command Sheet
    1. Set backend + device env
cd /home/neopeak/Desktop/WF-project/WF/Workforce-Detection nano backend/.env 
Set/check:
    • EDGE_API_BASE=http://<BACKEND_IP>:8000/api/v1
    • BACKEND_API_URL=http://<BACKEND_IP>:8000
    • EDGE_DEVICE_KEY=<DEVICE_API_KEY>
    • DEVICE_CODE=<DEVICE_CODE>
    • EDGE_PROCESSING_FPS=4
    • EDGE_FRAME_QUEUE_MAXSIZE=12
    • EDGE_EVENT_QUEUE_MAXSIZE=4000
    2. Verify backend reachability
curl http://<BACKEND_IP>:8000/health curl http://<BACKEND_IP>:8000/api/v1/edge/ping 
    3. Sync runtime config (must succeed)
/home/neopeak/torch_env/bin/python jetson/edge_config_sync.py 
    4. Quick config sanity
python - <<'PY' import json p='jetson/config/local_cache.json' d=json.load(open(p)) print("cameras:",len(d["cameras"])) for c in d["cameras"]: print(c["code"], c.get("stream_type"), c.get("decode_mode"), c.get("fps"), c.get("rtsp_url")) PY 
    5. Install/refresh systemd units (one-time or after edits)
sudo cp systemd/workforce-backend.service /etc/systemd/system/ sudo cp systemd/workforce-aggregator.service /etc/systemd/system/ sudo cp systemd/workforce-phase5.service /etc/systemd/system/ sudo cp systemd/workforce-phase5.timer /etc/systemd/system/ sudo cp systemd/workforce-edge.service /etc/systemd/system/ sudo cp systemd/workforce-heartbeat.service /etc/systemd/system/ sudo cp systemd/workforce-watchdog.service /etc/systemd/system/ sudo systemctl daemon-reload 
    6. Enable services
sudo systemctl enable workforce-backend sudo systemctl enable workforce-aggregator sudo systemctl enable workforce-phase5.timer sudo systemctl enable workforce-edge sudo systemctl enable workforce-heartbeat sudo systemctl enable workforce-watchdog 
    7. Start in order
sudo systemctl start workforce-backend sudo systemctl start workforce-aggregator sudo systemctl start workforce-phase5.timer sudo systemctl start workforce-edge sudo systemctl start workforce-heartbeat sudo systemctl start workforce-watchdog 
    8. Status snapshot
sudo systemctl --no-pager --full status workforce-backend workforce-aggregator workforce-phase5.timer workforce-edge workforce-heartbeat workforce-watchdog 
    9. Live logs (open in separate terminals)
journalctl -u workforce-edge -f journalctl -u workforce-heartbeat -f journalctl -u workforce-aggregator -f journalctl -u workforce-backend -f 
    10. 30-min health check
        ◦ workforce-edge logs show all 5 cameras active.
        ◦ No continuous queue pressure warnings.
        ◦ Heartbeat posts every ~120s.
        ◦ Aggregator keeps processing without growing backlog.
    11. If overloaded (quick rollback)
nano backend/.env # set EDGE_PROCESSING_FPS=3 sudo systemctl restart workforce-edge 
    12. If a service fails
sudo journalctl -u <service-name> -n 200 --no-pager 

============================================================================================================================================================

5. NEW FARM SETUP — FULL CHECKLIST
This is what you asked → exact actionable steps

STEP 1 — DATABASE SETUP
Create new farm
Insert into:
farm
Fields:
    • id 
    • name 
    • timezone (IMPORTANT) 

Create cameras
camera
For each:
    • id 
    • farm_id 
    • rtsp_url 
    • zone_config 

Create devices (Jetson)
device
Fields:
    • device_id 
    • farm_id 
    • api_key 

Create activity schedules
activity_schedule

STEP 2 — JETSON CONFIG
File:
jetson/config/bootstrap_config.json
Update:
{
  "device_id": "NEW_DEVICE_ID",
  "api_base_url": "http://backend-ip:port",
  "farm_id": "NEW_FARM_ID"
}

File:
jetson/config/local_cache.json
Delete or reset:
rm local_cache.json
(force fresh sync)

STEP 3 — NETWORK / WIFI
Change:
    • WiFi SSID 
    • IP changes 
Impact:
    • RTSP URLs may change 

Update RTSP in DB:
rtsp://username:password@NEW_IP:554/stream

STEP 4 — ENV CONFIG (BACKEND)
File:
backend/.env
Check:
    • DB connection (Supabase) 
    • JWT secret 
    • API host 

STEP 5 — MODEL DEPLOYMENT
Jetson:
models/
  WF_V1.3_best.engine
Ensure:
    • Same JetPack build 
    • Not copied from different device 

STEP 6 — ROI CONFIG
Folder:
jetson/roi_selection/
IMPORTANT RULE (you defined earlier):
ROI must be normalized (0–1)

For new farm:
    • Re-draw ROI 
    • Save normalized 

STEP 7 — START SERVICES
On Jetson:
sudo systemctl daemon-reexec
sudo systemctl daemon-reload
sudo systemctl enable workforce-edge
sudo systemctl enable workforce-heartbeat
sudo systemctl enable workforce-watchdog
sudo systemctl start workforce-edge
sudo systemctl start workforce-heartbeat
sudo systemctl start workforce-watchdog

STEP 8 — BACKEND SERVICES
sudo systemctl enable workforce-backend
sudo systemctl enable workforce-aggregator
sudo systemctl enable workforce-phase5
sudo systemctl start workforce-backend
sudo systemctl start workforce-aggregator
sudo systemctl start workforce-phase5

STEP 9 — VALIDATION
Check logs:
journalctl -u workforce-edge -f
journalctl -u workforce-heartbeat -f
journalctl -u workforce-aggregator -f

Must confirm:
✔ RTSP working
    • Frames incoming 
✔ Detection working
    • Objects detected 
✔ Events reaching backend
    • event_ingest_api hit 
✔ Aggregation working
    • activity_instance created 
✔ Phase5 working
    • status = IN_PROGRESS (not EARLY bug) 

